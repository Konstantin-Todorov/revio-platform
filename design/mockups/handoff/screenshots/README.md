# Product screenshots & videos

Drop real captures here. The site references these filenames — matching them is
the only step to swap the placeholders for real UI.

| File | Where it shows |
| --- | --- |
| `platform-overview.png` / `.mp4` | Home hero showcase |
| `link-home.png` | Home product card (RevioLink) + RevioLink page hero |
| `crs-home.png` | Home product card (RevioCRS) + RevioCRS page hero |
| `pms-home.png` | Home product card (RevioPMS) + RevioPMS page hero |

**Capture tips:** 1280×800, dark UI, hide any real tenant data. MP4s should be
short (5–10s), muted, and loop cleanly (autoplay muted loop is already wired).

Until files exist, the `BrowserFrame` component shows a themed placeholder — the
layout is final, so nothing shifts when you drop these in.
