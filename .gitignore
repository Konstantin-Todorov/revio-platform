# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.next/
out/
build/*.js
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# os
.DS_Store

# intake (raw source material, not part of the build)
_intake/

# railway
.railway/

# serena (local LSP index / tool state — not part of the build)
.serena/

# Locally stored uploads (dev object store). Production uses a bucket — never commit media.
.storage/

# Local backup output (database dumps + mirrored bucket objects — never commit these).
backups/
